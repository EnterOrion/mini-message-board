<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito+Sans&family=Roboto&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <h1><%= title %></h1>
    <% for (let i = 0; i < messages.length; i++) { %>
    <div class="message">
      <div class="text-user">
        <div class="message-text"><%= messages[i].message %></div>
        <div class="message-user"><strong><%= messages[i].user %></strong></div>
      </div>
      <div class="message-date"><em><%= messages[i].date_formatted%></em></div>
    </div>
    <% } %>
    <button><a href="/new">Submit a new message</a></button>
  </body>
</html>
